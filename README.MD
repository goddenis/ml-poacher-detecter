# Система обнаружения браконьеров (Poacher Detection System)

Автоматическое детектирование людей с оружием на фотоловушках с использованием компьютерного зрения и глубокого обучения.

---

## Содержание

- [Описание](#описание)
- [Технический стек](#технический-стек)
- [Структура проекта](#структура-проекта)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [Датасет](#датасет)
- [Обучение модели](#обучение-модели)
- [Инференс и оценка](#инференс-и-оценка)
- [Результаты](#результаты)
- [Использование](#использование)
- [Требования](#требования)
- [Лицензия](#лицензия)

---

## Описание

Эта система разработана для автоматического обнаружения потенциальных браконьеров (людей с оружием) на снимках с фотоловушек в защищённых природных территориях.

### Задача (Problem Statement)

На охраняемых территориях (национальные парки, заповедники) установлены **тысячи фотоловушек**, которые ежедневно производят **огромное количество изображений**. Ручной анализ всех этих фото крайне **времязатратен и неэффективен**.

**Проблемы:**
- Ежедневно тысячи фотографий требуют анализа
- Только 3-5% кадров содержат браконьеров (низкая частота)
- Задержки в реагировании (недели вместо минут)
- Высокие затраты на персонал

### Решение

**Система автоматического обнаружения** позволяет:
- Анализировать ВСЕ фотографии за часы вместо недель
- Оперативно реагировать на угрозы в реальном времени
- Круглосуточный мониторинг без усталости
- Накапливать статистику и тренды браконьерства
- Направлять патрули в нужные места

---

## Технический стек

### Модель и фреймворки
| Компонент | Версия | Роль |
|-----------|--------|------|
| **YOLOv8 Nano** | 8.0+ | Детектирование объектов |
| **PyTorch** | 2.0+ | Backend для глубокого обучения |
| **Ultralytics** | 8.0+ | High-level API для YOLOv8 |
| **OpenCV** | 4.8+ | Обработка изображений |
| **Pandas** | 2.0+ | Анализ данных и статистика |

### Вычислительные требования
- **GPU:** NVIDIA (рекомендуется 15 ГБ VRAM)
- **Модель:** YOLOv8 Nano (~3.3M параметров)
- **Скорость инференса:** 100+ fps на GPU
- **Потребление памяти:** 0.9-1.5 ГБ VRAM

### Датасет
- **Источник:** person-weapon-v2 (Roboflow)
- **Классы:** 5 (grenade, knife, pistol, rifle, person)
- **Размер:** 2000+ изображений
- **Format:** YOLO (txt аннотации)

---

## 📁 Структура проекта

```
poacher-detection-system/
│
├── 📄 README.md                        # Этот файл
├── 📄 requirements.txt                 # Python зависимости
├── 📄 .gitignore                       # Git исключения
│
├── 📄 BUSINESS_REQUIREMENTS.md         # Бизнес-постановка задачи
├── 📄 DS_APPROACH.md                   # DS-методология
│
├── 📁 data/
│   ├── raw/                            # Исходные изображения (Roboflow)
│   ├── processed/
│   │   └── yolo_format/
│   │       ├── train/                  # Обучающий набор (70%)
│   │       │   ├── images/
│   │       │   └── labels/
│   │       ├── val/                    # Валидационный набор (15%)
│   │       │   ├── images/
│   │       │   └── labels/
│   │       └── test/                   # Тестовый набор (15%)
│   │           ├── images/
│   │           └── labels/
│   ├── dataset.yaml                    # Конфиг датасета для YOLOv8
│   ├── metadata.json                   # Метаданные датасета
│   └── datasets_info.md                # Описание источников
│
├── 📁 models/
│   ├── yolov8n.pt                      # Предобученная модель
│   ├── yolov8n_best_v2.pt              # Лучшая модель после обучения
│   └── yolov8n_poacher_v2/             # Папка результатов обучения
│       ├── weights/
│       │   ├── best.pt
│       │   └── last.pt
│       └── results.csv                 # Метрики по эпохам
│
├── 📁 notebooks/
│   ├── 01_data_preparation.ipynb       # Сбор и подготовка данных
│   ├── 02_model_training.ipynb         # Обучение YOLOv8
│   └── 03_evaluation_and_demo.ipynb    # Оценка и демонстрация
│
└── 📁 results/
    ├── metrics.json                    # Итоговые метрики
    ├── confusion_matrix.png            # Матрица ошибок
    ├── training_curves.png             # Графики обучения
    └── sample_predictions/             # Примеры детекций
```

---

## Установка

### Требования

- Python 3.8+
- pip или conda
- Git
- CUDA 11.8+ (опционально, для GPU)

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/yourname/poacher-detection-system.git
cd poacher-detection-system
```

### Шаг 2: Создание виртуального окружения

```bash
# Для Linux/Mac
python -m venv venv
source venv/bin/activate

# Для Windows
python -m venv venv
venv\Scripts\activate
```

### Шаг 3: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 4: Проверка установки

```bash
python -c "import torch; print(f'PyTorch: {torch.__version__}')"
python -c "from ultralytics import YOLO; print('YOLOv8: OK')"
```

---

## ⚡ Быстрый старт

### Вариант 1: Запуск всех ноутбуков по порядку

```bash
# 1. Подготовка данных
jupyter notebook notebooks/01_data_preparation.ipynb

# 2. Обучение модели (2-4 часа на GPU)
jupyter notebook notebooks/02_model_training.ipynb

# 3. Оценка и демонстрация
jupyter notebook notebooks/03_evaluation_and_demo.ipynb
```

### Вариант 2: Использование предобученной модели

```python
from ultralytics import YOLO
from pathlib import Path

# Загрузить модель
model = YOLO('models/yolov8n_best_v2.pt')

# Инференс на изображении
results = model.predict(source='path/to/image.jpg', conf=0.5)

# Обработать результаты
for r in results:
    for box in r.boxes:
        class_id = int(box.cls)
        confidence = float(box.conf)
        print(f"Класс: {class_id}, Уверенность: {confidence:.2f}")
```

### Вариант 3: Определение браконьеров

```python
from ultralytics import YOLO

model = YOLO('models/yolov8n_best_v2.pt')

# Определение браконьеров на изображении
image_path = 'path/to/image.jpg'
results = model.predict(source=image_path, conf=0.5, verbose=False)

# Логика: Браконьер = Person (класс 4) + Оружие (классы 0,1,2,3)
WEAPON_CLASSES = {0, 1, 2, 3}  # grenade, knife, pistol, rifle
PERSON_CLASS = 4

for r in results:
    classes_in_image = set(int(box.cls) for box in r.boxes)
    has_person = PERSON_CLASS in classes_in_image
    has_weapon = any(w in classes_in_image for w in WEAPON_CLASSES)
    
    if has_person and has_weapon:
        print("БРАКОНЬЕР ОБНАРУЖЕН!")
        # Отправить алерт, логировать, т.д.
    else:
        print("Безопасно")
```

---

## Датасет

### person-weapon-v2 (5 классов)

| ID | Класс | Тип | Описание | Примеры |
|----|--------|-----|---------|---------|
| 0  | grenade | Оружие | Граната | 1413    |
| 1  | knife | Оружие | Нож | 845     |
| 2  | pistol | Оружие | Пистолет | 1010    |
| 3  | rifle | Оружие | Винтовка/карабин | 1227    |
| 4  | person | Человек | Человек на фото | 2257    |

### Типы фото в датасете

- **Браконьеры** (Person + Оружие): ~38.3% от всех фото
- **Только люди**: 0% от всех фото  
- **Только оружие**: ~61.7% от всех фото

### Загрузка датасета

```python
from roboflow import Roboflow

# Требуется API ключ (бесплатно на roboflow.com)
rf = Roboflow(api_key="YOUR_API_KEY")
project = rf.workspace().project("person-weapon-v2")
dataset = project.versions(1).download("yolov8")
```

---

## 🎓 Обучение модели

### Параметры обучения

| Параметр | Значение | Описание |
|----------|----------|---------|
| Модель | YOLOv8 Nano | 0.9 ГБ VRAM |
| Эпохи | 50 | Количество проходов по данным |
| Размер батча | 16 | Изображений на итерацию |
| Размер изображения | 640×640 | Входные размеры для модели |
| Learning rate | 0.01 | Начальная скорость обучения |
| Оптимизатор | SGD | Stochastic Gradient Descent |
| Аугментация | Да | Случайные трансформации |

### Процесс обучения

```python
from ultralytics import YOLO

# Загрузить предобученную модель
model = YOLO('yolov8n.pt')

# Fine-tuning на кастомных данных
results = model.train(
    data='data/dataset.yaml',
    epochs=50,
    imgsz=640,
    batch=16,
    device=0,  # GPU ID
    patience=20,  # Early stopping
    save=True,
    augment=True,
)
```

### Ожидаемый прогресс

| Этап | mAP50 | Precision | Recall | Время |
|------|-------|-----------|--------|-------|
| Инициализация | ~40% | ~50% | ~30% | — |
| 5 эпох | ~75% | ~80% | ~70% | ~30 мин |
| 20 эпох | ~92% | ~94% | ~90% | ~2 часа |
| 50 эпох | ≥95% | ≥95% | ≥93% | ~4 часа |

---

## 📈 Инференс и оценка

### Валидация на test сете

```python
from ultralytics import YOLO
from pathlib import Path

model = YOLO('models/yolov8n_best_v2.pt')

# Валидация
metrics = model.val(
    data='data/dataset.yaml',
    split='test',
    imgsz=640,
    batch=16,
    device=0,
    conf=0.5
)

print(f"mAP50: {metrics.box.map50:.4f}")
print(f"mAP50-95: {metrics.box.map:.4f}")
print(f"Precision: {metrics.box.mp:.4f}")
print(f"Recall: {metrics.box.mr:.4f}")
```

### Batch-обработка директории

```python
from ultralytics import YOLO
from pathlib import Path

model = YOLO('models/yolov8n_best_v2.pt')

# Обработать все изображения в папке
image_dir = Path('data/test_images/')
for img_file in image_dir.glob('*.jpg'):
    results = model.predict(source=str(img_file), conf=0.5)
    
    for r in results:
        print(f"Изображение: {img_file.name}")
        print(f"Объектов найдено: {len(r.boxes)}")
```

### Экспорт результатов

```python
import json
import pandas as pd

# Экспорт в JSON
results_dict = {
    'mAP50': 0.95,
    'Precision': 0.94,
    'Recall': 0.93,
    'model': 'yolov8n_best_v2.pt',
}

with open('results/metrics.json', 'w') as f:
    json.dump(results_dict, f, indent=2)

# Экспорт в CSV
detections_df = pd.DataFrame([
    {'image': 'photo1.jpg', 'is_poacher': True, 'confidence': 0.95},
    {'image': 'photo2.jpg', 'is_poacher': False, 'confidence': 0.02},
])
detections_df.to_csv('results/detections.csv', index=False)
```

---

## Результаты

### Ожидаемые метрики на test сете

| Метрика | Значение | Описание |
|---------|----------|---------|
| **mAP50** | ≥89%     | Точность с IoU ≥0.5 |
| **mAP50-95** | ≥58%     | Среднее качество по IoU 0.5-0.95 |
| **Precision** | ≥88%     | Доля верных детекций |
| **Recall** | ≥81%     | Доля найденных объектов |

### Распределение браконьеров в датасете

```
Всего тестовых фото: 565
├─ Браконьеры найдены: 154 (27.3%)
├─ Только люди: 54 (9.6%)
└─ Другое: 329 (58.2%)
```
## Использование

### API инференса

```python
from ultralytics import YOLO
import cv2

model = YOLO('models/yolov8n_best_v2.pt')

# Одно изображение
result = model.predict('image.jpg', conf=0.5)

# Видео
result = model.predict('video.mp4', conf=0.5, save=True)

# Вебкамера в реальном времени
result = model.predict(source=0, conf=0.5, save=False)

# Папка с изображениями
result = model.predict('folder_with_images/', conf=0.5)
```

### Программное определение браконьеров

```python
def detect_and_classify_poachers(image_path, model, class_names,
                                  weapon_classes={0,1,2,3}, person_class=4,
                                  conf_threshold=0.5):
    """
    Обнаружить и классифицировать браконьеров на изображении.

    Args:
        image_path: путь к изображению
        model: обученная модель YOLOv8
        class_names: словарь названий классов
        weapon_classes: набор ID классов оружия
        person_class: ID класса человека
        conf_threshold: порог уверенности

    Returns:
        {
            'is_poacher': bool,
            'detections': [{'class': str, 'confidence': float, 'bbox': list}],
            'weapons_found': list,
            'alert_level': str  # 'SAFE', 'PERSON', 'WEAPON', 'POACHER_ALERT'
        }
    """
    results = model.predict(str(image_path), conf=conf_threshold, verbose=False)

    detections = []
    weapons_found = []
    classes_in_image = set()

    for result in results:
        for box in result.boxes:
            class_id = int(box.cls)
            classes_in_image.add(class_id)

            detection = {
                'class': class_names[class_id],
                'confidence': float(box.conf),
                'bbox': [int(x) for x in box.xyxy[0].tolist()],
            }
            detections.append(detection)

            if class_id in weapon_classes:
                weapons_found.append(class_names[class_id])

    # Определить уровень угрозы
    has_person = person_class in classes_in_image
    has_weapon = any(w in classes_in_image for w in weapon_classes)

    if has_person and has_weapon:
        alert_level = 'POACHER_ALERT'
        is_poacher = True
    elif has_person:
        alert_level = 'PERSON'
        is_poacher = False
    elif has_weapon:
        alert_level = 'WEAPON'
        is_poacher = False
    else:
        alert_level = 'SAFE'
        is_poacher = False

    return {
        'is_poacher': is_poacher,
        'detections': detections,
        'weapons_found': list(set(weapons_found)),
        'alert_level': alert_level
    }

# Использование
model = YOLO('models/yolov8n_best_v2.pt')

# Анализировать одно изображение
result = detect_and_classify_poachers(
    'images.jpeg',
    model,
    CLASS_NAMES,
    weapon_classes={0,1,2,3},
    person_class=4
)
print(result)
if result['is_poacher']:
    print("🚨 БРАКОНЬЕР ОБНАРУЖЕН!")
else:
    print("✅ Безопасно")
```

---

## ⚙️ Требования

### Python пакеты

```
torch==2.0.0
torchvision==0.15.0
ultralytics==8.0.100
opencv-python==4.8.0.74
numpy==1.24.3
pandas==2.0.3
matplotlib==3.7.2
seaborn==0.12.2
Pillow==10.0.0
tqdm==4.65.0
pyyaml==6.0
roboflow==1.1.27  # опционально для загрузки датасета
```

### Аппаратные требования (рекомендуемые)

- **GPU:** NVIDIA A100, RTX 4090, RTX 3090 или эквивалент
- **VRAM:** 15 ГБ+ (для batch processing)
- **CPU:** Intel i7/i9 или AMD Ryzen 7/9
- **RAM:** 32 ГБ+
- **SSD:** 100 ГБ+ свободного места

### Минимальные требования

- **GPU:** NVIDIA GTX 1080 или лучше
- **VRAM:** 8 ГБ
- **CPU:** Intel i5 или эквивалент
- **RAM:** 16 ГБ
- **SSD:** 50 ГБ

---

## Решение проблем

### Проблема: Out of Memory (OOM)

```python
# Решение: Уменьшить batch размер
model.train(
    data='dataset.yaml',
    batch=8,  # вместо 16
    device=0
)

# Или использовать smaller модель
model = YOLO('yolov8s.pt')  # small вместо nano
```

### Проблема: Медленное обучение

```python
# Решение: Включить автоматическое масштабирование изображений
model.train(
    data='dataset.yaml',
    imgsz='auto',  # автоматический выбор размера
    device=0
)
```

### Проблема: Низкая точность

```
1. Проверьте качество датасета (разметка, дубликаты)
2. Увеличьте количество эпох: epochs=100
3. Добавьте аугментацию: augment=True
4. Используйте большую модель: yolov8s вместо yolov8n
5. Соберите больше данных
```

---

## Дополнительные ресурсы

### Документация
- [YOLOv8 Documentation](https://docs.ultralytics.com/)
- [PyTorch Docs](https://pytorch.org/docs/)
- [OpenCV Tutorials](https://docs.opencv.org/)

### Примеры кода
- `notebooks/01_data_preparation.ipynb` - Подготовка данных
- `notebooks/02_model_training.ipynb` - Обучение модели
- `notebooks/03_evaluation_and_demo.ipynb` - Оценка и демонстрация
- `notebooks/AIO_Poacher_Detection.ipynb` - Объединение ноут для загрузки и запуcка в Google Colab 

### Датасеты для обучения
- [Roboflow person-weapon-v2](https://universe.roboflow.com/ksdjflaskjf/person-weapon)
- [Hugging Face Weapons Detection](https://huggingface.co/datasets)
- [Kaggle Weapon Detection](https://www.kaggle.com/datasets)

---

## Лицензия

MIT License - см. файл [LICENSE](LICENSE)

---

## Авторы

- **Разработчик:** Богомолов Денис 
- **Технология:** YOLOv8 + PyTorch
- **Дата создания:** Декабрь 2025

---

## Контакты и поддержка

- **Email:** goddenis@yandex.ru
- **GitHub:** [@goddenis](https://github.com/goddenis)
- **Issues:** [GitHub Issues](https://github.com/goddenis/poacher-detection-system/issues)

---

## Если проект помог вам, поставьте звезду!

```
git star https://github.com/yourname/poacher-detection-system
```

